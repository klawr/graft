# git-cliff configuration.
#
# Versions and CHANGELOG.md are derived entirely from conventional commits and
# `v*` git tags — no cargo registry is involved. This is the correct model for
# a binary that is never published to crates.io.

[changelog]
header = """
# Changelog

All notable changes to this project will be documented in this file.
"""
body = """
{% if version %}\
## [{{ version | trim_start_matches(pat="v") }}] - {{ timestamp | date(format="%Y-%m-%d") }}
{% else %}\
## [Unreleased]
{% endif %}\
{% for group, commits in commits | group_by(attribute="group") %}
### {{ group | upper_first }}
{% for commit in commits %}\
- {{ commit.message | split(pat="\\n") | first | trim | upper_first }}
{% endfor %}\
{% endfor %}
"""
trim = true
footer = ""

[git]
conventional_commits = true
filter_unconventional = true
split_commits = false
# Map conventional types to Keep a Changelog-style sections. Release/CI/test
# noise is skipped so it never lands in the changelog.
commit_parsers = [
  { message = "^feat", group = "Added" },
  { message = "^fix", group = "Fixed" },
  { message = "^perf", group = "Performance" },
  { message = "^refactor", group = "Changed" },
  { message = "^doc", group = "Documentation" },
  { message = "^chore: release", skip = true },
  { message = "^chore\\(release\\)", skip = true },
  { message = "^chore", group = "Miscellaneous" },
  { message = "^ci", skip = true },
  { message = "^test", skip = true },
]
protect_breaking_commits = false
filter_commits = false
tag_pattern = "v[0-9]*"
topo_order = false
sort_commits = "oldest"

[bump]
# Pre-1.0 semantics: a feature bumps the minor (0.2.x -> 0.3.0); a breaking
# change bumps the minor too, rather than jumping to 1.0.0. Flip
# breaking_always_bump_major to true once you want breaking changes to cut a
# major release.
features_always_bump_minor = true
breaking_always_bump_major = false
